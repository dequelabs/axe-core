{
  "name": "axe-core-stencil-jest-test",
  "version": "1.0.0",
  "private": true,
  "description": "Diagnostic test: axe-core inside Stencil Jest (newSpecPage)",
  "scripts": {
    "test": "stencil test --spec --no-build",
    "test:verbose": "stencil test --spec --no-build -- --verbose"
  },
  "dependencies": {
    "axe-core": "file:../../"
  },
  "devDependencies": {
    "@stencil/core": "^4.0.0",
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "jest-cli": "^29.0.0"
  }
}
