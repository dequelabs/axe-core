{
  "id": "target-size",
  "evaluate": "target-size-evaluate",
  "options": {
    "minSpacing": 23.95
  },
  "metadata": {
    "impact": "serious",
    "messages": {
      "pass": "Control has sufficient size (${data.width}px width, ${data.height}px height)",
      "fail": {
        "default": "Element has insufficient size (${data.width}px width, ${data.height}px height)",
        "obscured": "Element has insufficient size because it is obscured (smallest space is ${data.width}px width by ${data.height}px height)"
      }
    }
  }
}
